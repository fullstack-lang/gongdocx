// generated by ng_file_service_ts
import { Injectable, Component, Inject } from '@angular/core';
import { HttpClientModule, HttpParams } from '@angular/common/http';
import { HttpClient, HttpHeaders } from '@angular/common/http';
import { DOCUMENT, Location } from '@angular/common'

/*
 * Behavior subject
 */
import { BehaviorSubject } from 'rxjs';
import { Observable, of } from 'rxjs';
import { catchError, map, tap } from 'rxjs/operators';

import { ParagraphPropertiesDB } from './paragraphproperties-db';

// insertion point for imports
import { NodeDB } from './node-db'

@Injectable({
  providedIn: 'root'
})
export class ParagraphPropertiesService {

  // Kamar Raïmo: Adding a way to communicate between components that share information
  // so that they are notified of a change.
  ParagraphPropertiesServiceChanged: BehaviorSubject<string> = new BehaviorSubject("");

  private paragraphpropertiessUrl: string

  constructor(
    private http: HttpClient,
    @Inject(DOCUMENT) private document: Document
  ) {
    // path to the service share the same origin with the path to the document
    // get the origin in the URL to the document
    let origin = this.document.location.origin

    // if debugging with ng, replace 4200 with 8080
    origin = origin.replace("4200", "8080")

    // compute path to the service
    this.paragraphpropertiessUrl = origin + '/api/github.com/fullstack-lang/gongdocx/go/v1/paragraphpropertiess';
  }

  /** GET paragraphpropertiess from the server */
  getParagraphPropertiess(GONG__StackPath: string): Observable<ParagraphPropertiesDB[]> {

    let params = new HttpParams().set("GONG__StackPath", GONG__StackPath)

    return this.http.get<ParagraphPropertiesDB[]>(this.paragraphpropertiessUrl, { params: params })
      .pipe(
        tap(),
		// tap(_ => this.log('fetched paragraphpropertiess')),
        catchError(this.handleError<ParagraphPropertiesDB[]>('getParagraphPropertiess', []))
      );
  }

  /** GET paragraphproperties by id. Will 404 if id not found */
  getParagraphProperties(id: number, GONG__StackPath: string): Observable<ParagraphPropertiesDB> {

    let params = new HttpParams().set("GONG__StackPath", GONG__StackPath)

    const url = `${this.paragraphpropertiessUrl}/${id}`;
    return this.http.get<ParagraphPropertiesDB>(url, { params: params }).pipe(
      // tap(_ => this.log(`fetched paragraphproperties id=${id}`)),
      catchError(this.handleError<ParagraphPropertiesDB>(`getParagraphProperties id=${id}`))
    );
  }

  /** POST: add a new paragraphproperties to the server */
  postParagraphProperties(paragraphpropertiesdb: ParagraphPropertiesDB, GONG__StackPath: string): Observable<ParagraphPropertiesDB> {

    // insertion point for reset of pointers and reverse pointers (to avoid circular JSON)
    paragraphpropertiesdb.Node = new NodeDB

    let params = new HttpParams().set("GONG__StackPath", GONG__StackPath)
    let httpOptions = {
      headers: new HttpHeaders({ 'Content-Type': 'application/json' }),
      params: params
    }

    return this.http.post<ParagraphPropertiesDB>(this.paragraphpropertiessUrl, paragraphpropertiesdb, httpOptions).pipe(
      tap(_ => {
        // insertion point for restoration of reverse pointers
        // this.log(`posted paragraphpropertiesdb id=${paragraphpropertiesdb.ID}`)
      }),
      catchError(this.handleError<ParagraphPropertiesDB>('postParagraphProperties'))
    );
  }

  /** DELETE: delete the paragraphpropertiesdb from the server */
  deleteParagraphProperties(paragraphpropertiesdb: ParagraphPropertiesDB | number, GONG__StackPath: string): Observable<ParagraphPropertiesDB> {
    const id = typeof paragraphpropertiesdb === 'number' ? paragraphpropertiesdb : paragraphpropertiesdb.ID;
    const url = `${this.paragraphpropertiessUrl}/${id}`;

    let params = new HttpParams().set("GONG__StackPath", GONG__StackPath)
    let httpOptions = {
      headers: new HttpHeaders({ 'Content-Type': 'application/json' }),
      params: params
    };

    return this.http.delete<ParagraphPropertiesDB>(url, httpOptions).pipe(
      tap(_ => this.log(`deleted paragraphpropertiesdb id=${id}`)),
      catchError(this.handleError<ParagraphPropertiesDB>('deleteParagraphProperties'))
    );
  }

  /** PUT: update the paragraphpropertiesdb on the server */
  updateParagraphProperties(paragraphpropertiesdb: ParagraphPropertiesDB, GONG__StackPath: string): Observable<ParagraphPropertiesDB> {
    const id = typeof paragraphpropertiesdb === 'number' ? paragraphpropertiesdb : paragraphpropertiesdb.ID;
    const url = `${this.paragraphpropertiessUrl}/${id}`;

    // insertion point for reset of pointers and reverse pointers (to avoid circular JSON)
    paragraphpropertiesdb.Node = new NodeDB

    let params = new HttpParams().set("GONG__StackPath", GONG__StackPath)
    let httpOptions = {
      headers: new HttpHeaders({ 'Content-Type': 'application/json' }),
      params: params
    };

    return this.http.put<ParagraphPropertiesDB>(url, paragraphpropertiesdb, httpOptions).pipe(
      tap(_ => {
        // insertion point for restoration of reverse pointers
        // this.log(`updated paragraphpropertiesdb id=${paragraphpropertiesdb.ID}`)
      }),
      catchError(this.handleError<ParagraphPropertiesDB>('updateParagraphProperties'))
    );
  }

  /**
   * Handle Http operation that failed.
   * Let the app continue.
   * @param operation - name of the operation that failed
   * @param result - optional value to return as the observable result
   */
  private handleError<T>(operation = 'operation in ParagraphPropertiesService', result?: T) {
    return (error: any): Observable<T> => {

      // TODO: send the error to remote logging infrastructure
      console.error("ParagraphPropertiesService" + error); // log to console instead

      // TODO: better job of transforming error for user consumption
      this.log(`${operation} failed: ${error.message}`);

      // Let the app keep running by returning an empty result.
      return of(result as T);
    };
  }

  private log(message: string) {
      console.log(message)
  }
}
