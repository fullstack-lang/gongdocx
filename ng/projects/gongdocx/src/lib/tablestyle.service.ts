// generated by ng_file_service_ts
import { Injectable, Component, Inject } from '@angular/core';
import { HttpClientModule, HttpParams } from '@angular/common/http';
import { HttpClient, HttpHeaders } from '@angular/common/http';
import { DOCUMENT, Location } from '@angular/common'

/*
 * Behavior subject
 */
import { BehaviorSubject } from 'rxjs';
import { Observable, of } from 'rxjs';
import { catchError, map, tap } from 'rxjs/operators';

import { TableStyleDB } from './tablestyle-db';

// insertion point for imports
import { NodeDB } from './node-db'

@Injectable({
  providedIn: 'root'
})
export class TableStyleService {

  // Kamar Raïmo: Adding a way to communicate between components that share information
  // so that they are notified of a change.
  TableStyleServiceChanged: BehaviorSubject<string> = new BehaviorSubject("");

  private tablestylesUrl: string

  constructor(
    private http: HttpClient,
    @Inject(DOCUMENT) private document: Document
  ) {
    // path to the service share the same origin with the path to the document
    // get the origin in the URL to the document
    let origin = this.document.location.origin

    // if debugging with ng, replace 4200 with 8080
    origin = origin.replace("4200", "8080")

    // compute path to the service
    this.tablestylesUrl = origin + '/api/github.com/fullstack-lang/gongdocx/go/v1/tablestyles';
  }

  /** GET tablestyles from the server */
  getTableStyles(GONG__StackPath: string): Observable<TableStyleDB[]> {

    let params = new HttpParams().set("GONG__StackPath", GONG__StackPath)

    return this.http.get<TableStyleDB[]>(this.tablestylesUrl, { params: params })
      .pipe(
        tap(),
		// tap(_ => this.log('fetched tablestyles')),
        catchError(this.handleError<TableStyleDB[]>('getTableStyles', []))
      );
  }

  /** GET tablestyle by id. Will 404 if id not found */
  getTableStyle(id: number, GONG__StackPath: string): Observable<TableStyleDB> {

    let params = new HttpParams().set("GONG__StackPath", GONG__StackPath)

    const url = `${this.tablestylesUrl}/${id}`;
    return this.http.get<TableStyleDB>(url, { params: params }).pipe(
      // tap(_ => this.log(`fetched tablestyle id=${id}`)),
      catchError(this.handleError<TableStyleDB>(`getTableStyle id=${id}`))
    );
  }

  /** POST: add a new tablestyle to the server */
  postTableStyle(tablestyledb: TableStyleDB, GONG__StackPath: string): Observable<TableStyleDB> {

    // insertion point for reset of pointers and reverse pointers (to avoid circular JSON)
    tablestyledb.Node = new NodeDB

    let params = new HttpParams().set("GONG__StackPath", GONG__StackPath)
    let httpOptions = {
      headers: new HttpHeaders({ 'Content-Type': 'application/json' }),
      params: params
    }

    return this.http.post<TableStyleDB>(this.tablestylesUrl, tablestyledb, httpOptions).pipe(
      tap(_ => {
        // insertion point for restoration of reverse pointers
        // this.log(`posted tablestyledb id=${tablestyledb.ID}`)
      }),
      catchError(this.handleError<TableStyleDB>('postTableStyle'))
    );
  }

  /** DELETE: delete the tablestyledb from the server */
  deleteTableStyle(tablestyledb: TableStyleDB | number, GONG__StackPath: string): Observable<TableStyleDB> {
    const id = typeof tablestyledb === 'number' ? tablestyledb : tablestyledb.ID;
    const url = `${this.tablestylesUrl}/${id}`;

    let params = new HttpParams().set("GONG__StackPath", GONG__StackPath)
    let httpOptions = {
      headers: new HttpHeaders({ 'Content-Type': 'application/json' }),
      params: params
    };

    return this.http.delete<TableStyleDB>(url, httpOptions).pipe(
      tap(_ => this.log(`deleted tablestyledb id=${id}`)),
      catchError(this.handleError<TableStyleDB>('deleteTableStyle'))
    );
  }

  /** PUT: update the tablestyledb on the server */
  updateTableStyle(tablestyledb: TableStyleDB, GONG__StackPath: string): Observable<TableStyleDB> {
    const id = typeof tablestyledb === 'number' ? tablestyledb : tablestyledb.ID;
    const url = `${this.tablestylesUrl}/${id}`;

    // insertion point for reset of pointers and reverse pointers (to avoid circular JSON)
    tablestyledb.Node = new NodeDB

    let params = new HttpParams().set("GONG__StackPath", GONG__StackPath)
    let httpOptions = {
      headers: new HttpHeaders({ 'Content-Type': 'application/json' }),
      params: params
    };

    return this.http.put<TableStyleDB>(url, tablestyledb, httpOptions).pipe(
      tap(_ => {
        // insertion point for restoration of reverse pointers
        // this.log(`updated tablestyledb id=${tablestyledb.ID}`)
      }),
      catchError(this.handleError<TableStyleDB>('updateTableStyle'))
    );
  }

  /**
   * Handle Http operation that failed.
   * Let the app continue.
   * @param operation - name of the operation that failed
   * @param result - optional value to return as the observable result
   */
  private handleError<T>(operation = 'operation in TableStyleService', result?: T) {
    return (error: any): Observable<T> => {

      // TODO: send the error to remote logging infrastructure
      console.error("TableStyleService" + error); // log to console instead

      // TODO: better job of transforming error for user consumption
      this.log(`${operation} failed: ${error.message}`);

      // Let the app keep running by returning an empty result.
      return of(result as T);
    };
  }

  private log(message: string) {
      console.log(message)
  }
}
