// generated by ng_file_service_ts
import { Injectable, Component, Inject } from '@angular/core';
import { HttpClientModule, HttpParams } from '@angular/common/http';
import { HttpClient, HttpHeaders } from '@angular/common/http';
import { DOCUMENT, Location } from '@angular/common'

/*
 * Behavior subject
 */
import { BehaviorSubject } from 'rxjs';
import { Observable, of } from 'rxjs';
import { catchError, map, tap } from 'rxjs/operators';

import { TablePropertiesDB } from './tableproperties-db';

// insertion point for imports
import { NodeDB } from './node-db'
import { TableStyleDB } from './tablestyle-db'

@Injectable({
  providedIn: 'root'
})
export class TablePropertiesService {

  // Kamar Raïmo: Adding a way to communicate between components that share information
  // so that they are notified of a change.
  TablePropertiesServiceChanged: BehaviorSubject<string> = new BehaviorSubject("");

  private tablepropertiessUrl: string

  constructor(
    private http: HttpClient,
    @Inject(DOCUMENT) private document: Document
  ) {
    // path to the service share the same origin with the path to the document
    // get the origin in the URL to the document
    let origin = this.document.location.origin

    // if debugging with ng, replace 4200 with 8080
    origin = origin.replace("4200", "8080")

    // compute path to the service
    this.tablepropertiessUrl = origin + '/api/github.com/fullstack-lang/gongdocx/go/v1/tablepropertiess';
  }

  /** GET tablepropertiess from the server */
  getTablePropertiess(GONG__StackPath: string): Observable<TablePropertiesDB[]> {

    let params = new HttpParams().set("GONG__StackPath", GONG__StackPath)

    return this.http.get<TablePropertiesDB[]>(this.tablepropertiessUrl, { params: params })
      .pipe(
        tap(),
		// tap(_ => this.log('fetched tablepropertiess')),
        catchError(this.handleError<TablePropertiesDB[]>('getTablePropertiess', []))
      );
  }

  /** GET tableproperties by id. Will 404 if id not found */
  getTableProperties(id: number, GONG__StackPath: string): Observable<TablePropertiesDB> {

    let params = new HttpParams().set("GONG__StackPath", GONG__StackPath)

    const url = `${this.tablepropertiessUrl}/${id}`;
    return this.http.get<TablePropertiesDB>(url, { params: params }).pipe(
      // tap(_ => this.log(`fetched tableproperties id=${id}`)),
      catchError(this.handleError<TablePropertiesDB>(`getTableProperties id=${id}`))
    );
  }

  /** POST: add a new tableproperties to the server */
  postTableProperties(tablepropertiesdb: TablePropertiesDB, GONG__StackPath: string): Observable<TablePropertiesDB> {

    // insertion point for reset of pointers and reverse pointers (to avoid circular JSON)
    tablepropertiesdb.Node = new NodeDB
    tablepropertiesdb.TableStyle = new TableStyleDB

    let params = new HttpParams().set("GONG__StackPath", GONG__StackPath)
    let httpOptions = {
      headers: new HttpHeaders({ 'Content-Type': 'application/json' }),
      params: params
    }

    return this.http.post<TablePropertiesDB>(this.tablepropertiessUrl, tablepropertiesdb, httpOptions).pipe(
      tap(_ => {
        // insertion point for restoration of reverse pointers
        // this.log(`posted tablepropertiesdb id=${tablepropertiesdb.ID}`)
      }),
      catchError(this.handleError<TablePropertiesDB>('postTableProperties'))
    );
  }

  /** DELETE: delete the tablepropertiesdb from the server */
  deleteTableProperties(tablepropertiesdb: TablePropertiesDB | number, GONG__StackPath: string): Observable<TablePropertiesDB> {
    const id = typeof tablepropertiesdb === 'number' ? tablepropertiesdb : tablepropertiesdb.ID;
    const url = `${this.tablepropertiessUrl}/${id}`;

    let params = new HttpParams().set("GONG__StackPath", GONG__StackPath)
    let httpOptions = {
      headers: new HttpHeaders({ 'Content-Type': 'application/json' }),
      params: params
    };

    return this.http.delete<TablePropertiesDB>(url, httpOptions).pipe(
      tap(_ => this.log(`deleted tablepropertiesdb id=${id}`)),
      catchError(this.handleError<TablePropertiesDB>('deleteTableProperties'))
    );
  }

  /** PUT: update the tablepropertiesdb on the server */
  updateTableProperties(tablepropertiesdb: TablePropertiesDB, GONG__StackPath: string): Observable<TablePropertiesDB> {
    const id = typeof tablepropertiesdb === 'number' ? tablepropertiesdb : tablepropertiesdb.ID;
    const url = `${this.tablepropertiessUrl}/${id}`;

    // insertion point for reset of pointers and reverse pointers (to avoid circular JSON)
    tablepropertiesdb.Node = new NodeDB
    tablepropertiesdb.TableStyle = new TableStyleDB

    let params = new HttpParams().set("GONG__StackPath", GONG__StackPath)
    let httpOptions = {
      headers: new HttpHeaders({ 'Content-Type': 'application/json' }),
      params: params
    };

    return this.http.put<TablePropertiesDB>(url, tablepropertiesdb, httpOptions).pipe(
      tap(_ => {
        // insertion point for restoration of reverse pointers
        // this.log(`updated tablepropertiesdb id=${tablepropertiesdb.ID}`)
      }),
      catchError(this.handleError<TablePropertiesDB>('updateTableProperties'))
    );
  }

  /**
   * Handle Http operation that failed.
   * Let the app continue.
   * @param operation - name of the operation that failed
   * @param result - optional value to return as the observable result
   */
  private handleError<T>(operation = 'operation in TablePropertiesService', result?: T) {
    return (error: any): Observable<T> => {

      // TODO: send the error to remote logging infrastructure
      console.error("TablePropertiesService" + error); // log to console instead

      // TODO: better job of transforming error for user consumption
      this.log(`${operation} failed: ${error.message}`);

      // Let the app keep running by returning an empty result.
      return of(result as T);
    };
  }

  private log(message: string) {
      console.log(message)
  }
}
