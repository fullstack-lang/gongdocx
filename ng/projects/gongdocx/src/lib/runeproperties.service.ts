// generated by ng_file_service_ts
import { Injectable, Component, Inject } from '@angular/core';
import { HttpClientModule, HttpParams } from '@angular/common/http';
import { HttpClient, HttpHeaders } from '@angular/common/http';
import { DOCUMENT, Location } from '@angular/common'

/*
 * Behavior subject
 */
import { BehaviorSubject } from 'rxjs';
import { Observable, of } from 'rxjs';
import { catchError, map, tap } from 'rxjs/operators';

import { RunePropertiesDB } from './runeproperties-db';

// insertion point for imports
import { NodeDB } from './node-db'

@Injectable({
  providedIn: 'root'
})
export class RunePropertiesService {

  // Kamar Raïmo: Adding a way to communicate between components that share information
  // so that they are notified of a change.
  RunePropertiesServiceChanged: BehaviorSubject<string> = new BehaviorSubject("");

  private runepropertiessUrl: string

  constructor(
    private http: HttpClient,
    @Inject(DOCUMENT) private document: Document
  ) {
    // path to the service share the same origin with the path to the document
    // get the origin in the URL to the document
    let origin = this.document.location.origin

    // if debugging with ng, replace 4200 with 8080
    origin = origin.replace("4200", "8080")

    // compute path to the service
    this.runepropertiessUrl = origin + '/api/github.com/fullstack-lang/gongdocx/go/v1/runepropertiess';
  }

  /** GET runepropertiess from the server */
  getRunePropertiess(GONG__StackPath: string): Observable<RunePropertiesDB[]> {

    let params = new HttpParams().set("GONG__StackPath", GONG__StackPath)

    return this.http.get<RunePropertiesDB[]>(this.runepropertiessUrl, { params: params })
      .pipe(
        tap(),
		// tap(_ => this.log('fetched runepropertiess')),
        catchError(this.handleError<RunePropertiesDB[]>('getRunePropertiess', []))
      );
  }

  /** GET runeproperties by id. Will 404 if id not found */
  getRuneProperties(id: number, GONG__StackPath: string): Observable<RunePropertiesDB> {

    let params = new HttpParams().set("GONG__StackPath", GONG__StackPath)

    const url = `${this.runepropertiessUrl}/${id}`;
    return this.http.get<RunePropertiesDB>(url, { params: params }).pipe(
      // tap(_ => this.log(`fetched runeproperties id=${id}`)),
      catchError(this.handleError<RunePropertiesDB>(`getRuneProperties id=${id}`))
    );
  }

  /** POST: add a new runeproperties to the server */
  postRuneProperties(runepropertiesdb: RunePropertiesDB, GONG__StackPath: string): Observable<RunePropertiesDB> {

    // insertion point for reset of pointers and reverse pointers (to avoid circular JSON)
    runepropertiesdb.Node = new NodeDB

    let params = new HttpParams().set("GONG__StackPath", GONG__StackPath)
    let httpOptions = {
      headers: new HttpHeaders({ 'Content-Type': 'application/json' }),
      params: params
    }

    return this.http.post<RunePropertiesDB>(this.runepropertiessUrl, runepropertiesdb, httpOptions).pipe(
      tap(_ => {
        // insertion point for restoration of reverse pointers
        // this.log(`posted runepropertiesdb id=${runepropertiesdb.ID}`)
      }),
      catchError(this.handleError<RunePropertiesDB>('postRuneProperties'))
    );
  }

  /** DELETE: delete the runepropertiesdb from the server */
  deleteRuneProperties(runepropertiesdb: RunePropertiesDB | number, GONG__StackPath: string): Observable<RunePropertiesDB> {
    const id = typeof runepropertiesdb === 'number' ? runepropertiesdb : runepropertiesdb.ID;
    const url = `${this.runepropertiessUrl}/${id}`;

    let params = new HttpParams().set("GONG__StackPath", GONG__StackPath)
    let httpOptions = {
      headers: new HttpHeaders({ 'Content-Type': 'application/json' }),
      params: params
    };

    return this.http.delete<RunePropertiesDB>(url, httpOptions).pipe(
      tap(_ => this.log(`deleted runepropertiesdb id=${id}`)),
      catchError(this.handleError<RunePropertiesDB>('deleteRuneProperties'))
    );
  }

  /** PUT: update the runepropertiesdb on the server */
  updateRuneProperties(runepropertiesdb: RunePropertiesDB, GONG__StackPath: string): Observable<RunePropertiesDB> {
    const id = typeof runepropertiesdb === 'number' ? runepropertiesdb : runepropertiesdb.ID;
    const url = `${this.runepropertiessUrl}/${id}`;

    // insertion point for reset of pointers and reverse pointers (to avoid circular JSON)
    runepropertiesdb.Node = new NodeDB

    let params = new HttpParams().set("GONG__StackPath", GONG__StackPath)
    let httpOptions = {
      headers: new HttpHeaders({ 'Content-Type': 'application/json' }),
      params: params
    };

    return this.http.put<RunePropertiesDB>(url, runepropertiesdb, httpOptions).pipe(
      tap(_ => {
        // insertion point for restoration of reverse pointers
        // this.log(`updated runepropertiesdb id=${runepropertiesdb.ID}`)
      }),
      catchError(this.handleError<RunePropertiesDB>('updateRuneProperties'))
    );
  }

  /**
   * Handle Http operation that failed.
   * Let the app continue.
   * @param operation - name of the operation that failed
   * @param result - optional value to return as the observable result
   */
  private handleError<T>(operation = 'operation in RunePropertiesService', result?: T) {
    return (error: any): Observable<T> => {

      // TODO: send the error to remote logging infrastructure
      console.error("RunePropertiesService" + error); // log to console instead

      // TODO: better job of transforming error for user consumption
      this.log(`${operation} failed: ${error.message}`);

      // Let the app keep running by returning an empty result.
      return of(result as T);
    };
  }

  private log(message: string) {
      console.log(message)
  }
}
