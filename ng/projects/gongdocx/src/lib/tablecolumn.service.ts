// generated by ng_file_service_ts
import { Injectable, Component, Inject } from '@angular/core';
import { HttpClientModule, HttpParams } from '@angular/common/http';
import { HttpClient, HttpHeaders } from '@angular/common/http';
import { DOCUMENT, Location } from '@angular/common'

/*
 * Behavior subject
 */
import { BehaviorSubject } from 'rxjs';
import { Observable, of } from 'rxjs';
import { catchError, map, tap } from 'rxjs/operators';

import { TableColumnDB } from './tablecolumn-db';

// insertion point for imports
import { NodeDB } from './node-db'
import { TableRowDB } from './tablerow-db'

@Injectable({
  providedIn: 'root'
})
export class TableColumnService {

  // Kamar Raïmo: Adding a way to communicate between components that share information
  // so that they are notified of a change.
  TableColumnServiceChanged: BehaviorSubject<string> = new BehaviorSubject("");

  private tablecolumnsUrl: string

  constructor(
    private http: HttpClient,
    @Inject(DOCUMENT) private document: Document
  ) {
    // path to the service share the same origin with the path to the document
    // get the origin in the URL to the document
    let origin = this.document.location.origin

    // if debugging with ng, replace 4200 with 8080
    origin = origin.replace("4200", "8080")

    // compute path to the service
    this.tablecolumnsUrl = origin + '/api/github.com/fullstack-lang/gongdocx/go/v1/tablecolumns';
  }

  /** GET tablecolumns from the server */
  getTableColumns(GONG__StackPath: string): Observable<TableColumnDB[]> {

    let params = new HttpParams().set("GONG__StackPath", GONG__StackPath)

    return this.http.get<TableColumnDB[]>(this.tablecolumnsUrl, { params: params })
      .pipe(
        tap(),
		// tap(_ => this.log('fetched tablecolumns')),
        catchError(this.handleError<TableColumnDB[]>('getTableColumns', []))
      );
  }

  /** GET tablecolumn by id. Will 404 if id not found */
  getTableColumn(id: number, GONG__StackPath: string): Observable<TableColumnDB> {

    let params = new HttpParams().set("GONG__StackPath", GONG__StackPath)

    const url = `${this.tablecolumnsUrl}/${id}`;
    return this.http.get<TableColumnDB>(url, { params: params }).pipe(
      // tap(_ => this.log(`fetched tablecolumn id=${id}`)),
      catchError(this.handleError<TableColumnDB>(`getTableColumn id=${id}`))
    );
  }

  /** POST: add a new tablecolumn to the server */
  postTableColumn(tablecolumndb: TableColumnDB, GONG__StackPath: string): Observable<TableColumnDB> {

    // insertion point for reset of pointers and reverse pointers (to avoid circular JSON)
    tablecolumndb.Node = new NodeDB
    tablecolumndb.Paragraphs = []
    let _TableRow_TableColumns_reverse = tablecolumndb.TableRow_TableColumns_reverse
    tablecolumndb.TableRow_TableColumns_reverse = new TableRowDB

    let params = new HttpParams().set("GONG__StackPath", GONG__StackPath)
    let httpOptions = {
      headers: new HttpHeaders({ 'Content-Type': 'application/json' }),
      params: params
    }

    return this.http.post<TableColumnDB>(this.tablecolumnsUrl, tablecolumndb, httpOptions).pipe(
      tap(_ => {
        // insertion point for restoration of reverse pointers
        tablecolumndb.TableRow_TableColumns_reverse = _TableRow_TableColumns_reverse
        // this.log(`posted tablecolumndb id=${tablecolumndb.ID}`)
      }),
      catchError(this.handleError<TableColumnDB>('postTableColumn'))
    );
  }

  /** DELETE: delete the tablecolumndb from the server */
  deleteTableColumn(tablecolumndb: TableColumnDB | number, GONG__StackPath: string): Observable<TableColumnDB> {
    const id = typeof tablecolumndb === 'number' ? tablecolumndb : tablecolumndb.ID;
    const url = `${this.tablecolumnsUrl}/${id}`;

    let params = new HttpParams().set("GONG__StackPath", GONG__StackPath)
    let httpOptions = {
      headers: new HttpHeaders({ 'Content-Type': 'application/json' }),
      params: params
    };

    return this.http.delete<TableColumnDB>(url, httpOptions).pipe(
      tap(_ => this.log(`deleted tablecolumndb id=${id}`)),
      catchError(this.handleError<TableColumnDB>('deleteTableColumn'))
    );
  }

  /** PUT: update the tablecolumndb on the server */
  updateTableColumn(tablecolumndb: TableColumnDB, GONG__StackPath: string): Observable<TableColumnDB> {
    const id = typeof tablecolumndb === 'number' ? tablecolumndb : tablecolumndb.ID;
    const url = `${this.tablecolumnsUrl}/${id}`;

    // insertion point for reset of pointers and reverse pointers (to avoid circular JSON)
    tablecolumndb.Node = new NodeDB
    tablecolumndb.Paragraphs = []
    let _TableRow_TableColumns_reverse = tablecolumndb.TableRow_TableColumns_reverse
    tablecolumndb.TableRow_TableColumns_reverse = new TableRowDB

    let params = new HttpParams().set("GONG__StackPath", GONG__StackPath)
    let httpOptions = {
      headers: new HttpHeaders({ 'Content-Type': 'application/json' }),
      params: params
    };

    return this.http.put<TableColumnDB>(url, tablecolumndb, httpOptions).pipe(
      tap(_ => {
        // insertion point for restoration of reverse pointers
        tablecolumndb.TableRow_TableColumns_reverse = _TableRow_TableColumns_reverse
        // this.log(`updated tablecolumndb id=${tablecolumndb.ID}`)
      }),
      catchError(this.handleError<TableColumnDB>('updateTableColumn'))
    );
  }

  /**
   * Handle Http operation that failed.
   * Let the app continue.
   * @param operation - name of the operation that failed
   * @param result - optional value to return as the observable result
   */
  private handleError<T>(operation = 'operation in TableColumnService', result?: T) {
    return (error: any): Observable<T> => {

      // TODO: send the error to remote logging infrastructure
      console.error("TableColumnService" + error); // log to console instead

      // TODO: better job of transforming error for user consumption
      this.log(`${operation} failed: ${error.message}`);

      // Let the app keep running by returning an empty result.
      return of(result as T);
    };
  }

  private log(message: string) {
      console.log(message)
  }
}
